# Copy to .env — never commit .env
# Used by your deployment platform (Vercel, Render, Railway, etc.)
DATABASE_URL=jdbc:postgresql://host:5432/dbname
DB_USER=postgres
DB_PASSWORD=your_password
CORS_ORIGINS=https://your-frontend.vercel.app
DDL_AUTO=validate
